apiVersion: secrets-store.csi.x-k8s.io/v1alpha1
kind: SecretProviderClass
metadata:
  name: azure-kvname
spec:
  provider: azure
  parameters:
    tenantId: "f32b97f0-efb8-4bc3-91ee-18a6e5f635c9" # the tenant ID of the KeyVault
    keyvaultName: "ais-dz-k8s-vault"                 # the name of the KeyVault
    usePodIdentity: "false"
    useVMManagedIdentity: "false"
    userAssignedIdentityID: ""
    cloudName: ""
    cloudEnvFileName: ""
    objects:  |
      array:
        - |
          objectName: aks-https
          objectAlias: https.pfx.base64
          objectType: secret        # object types: secret, key or cert
          objectFormat: pfx         # for .NET we want the PFX format
          objectEncoding: base64
          objectVersion: ""         # [OPTIONAL] object versions, default to latest if empty
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: aks-keyvault-aspnetcore-win
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aks-keyvault-aspnetcore-win-pod
  template:
    metadata:
      labels:
        app: aks-keyvault-aspnetcore-win-pod
    spec:
      nodeSelector:
        "beta.kubernetes.io/os": windows
      containers:
      - name: aks-keyvault-aspnetcore-win-pod
        image: "aisdzcsicr.azurecr.io/aspnet-keyvault-win"
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 80
        - name: https
          containerPort: 443
        env:
        - name: HTTPS_CERTIFICATE_PATH
          value: "C:\\certs\\https.pfx.base64"
        volumeMounts:
        - name: aks-keyvault-aspnetcore-win-httpscert
          mountPath: /certs
          readOnly: true
        resources:
          requests:
            cpu: 500m
          limits:
            memory: 500Mi
            cpu: 1
      volumes:
      - name: aks-keyvault-aspnetcore-win-httpscert
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: "azure-kvname"
          nodePublishSecretRef:
            name: kvcreds
---
apiVersion: v1
kind: Service
metadata:
  name: aks-keyvault-aspnetcore-win-svc
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: http
  - name: https
    port: 443
    targetPort: https
  selector:
    app: aks-keyvault-aspnetcore-win-pod
